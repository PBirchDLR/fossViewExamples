using skyarcd
using haystack
using folio

@ExtMeta {
  name    = "fossViewExamples"
  depends = Str[,]
}
public const class FossViewExamplesExt : Ext {
  @NoDoc new make() : super() { }

  override Void onStart() {

  }
}